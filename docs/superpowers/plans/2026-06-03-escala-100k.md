# Escala 100k — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sustentar 100k+ pedidos/loja: sync sem array gigante em memória (RPC de escopo), push com pré-busca em lote (merge intacto), export CSV em streaming, e o último KPI agregado no banco.

**Architecture:** RPCs `SECURITY DEFINER` (mesmo padrão do `sync_parent_in_empresa` existente) fazem o escopo das filhas e a checagem de pais em lote no SQL. O `runPush` pré-carrega canônicas/pais em Maps/Sets antes do loop — a lógica de merge fica intacta. Export vira `ReadableStream` paginado. KPI tempo-médio vira RPC.

**Tech Stack:** Postgres/Supabase (RPC), Next.js 16, supabase-js, Vitest.

**Spec:** [docs/superpowers/specs/2026-06-03-escala-100k-design.md](../specs/2026-06-03-escala-100k-design.md)

**Deploy:** SQL aplica na nuvem (usuário roda) + hub via _hub_migrations. App via Vercel. **NÃO** forçar auto-update do hub (vai no próximo reinstall). Contrato hub↔nuvem preservado.

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `supabase/migrations/20260603140000_escala_100k.sql` | RPCs `sync_children_changed`, `sync_parents_in_empresa` (DEFINER+revoke), `admin_tempo_medio_horas` (INVOKER+grant). |
| `lib/types/database.ts` | Tipos das 3 RPCs. |
| `lib/sync/engine.ts` | `SyncDb` +`findCanonicalMany`/`parentsInEmpresa`; `runPush` pré-busca. |
| `lib/sync/supabase-db.ts` | `selectChanges` filhas via RPC; +`findCanonicalMany`/`parentsInEmpresa`; −`*IdsDaEmpresa`. |
| `lib/sync/__tests__/engine.test.ts`, `scenarios.test.ts` | Fake DB +2 métodos; teste de equivalência lote↔per-row. |
| `app/(app)/historico/export/route.ts` | Export CSV em streaming. |
| `app/(app)/admin/page.tsx` | KPI tempo médio via RPC. |

---

## Task 1: Migration (RPCs) + tipos

**Files:**
- Create: `supabase/migrations/20260603140000_escala_100k.sql`
- Modify: `lib/types/database.ts`

- [ ] **Step 1: Escrever a migration**

```sql
-- 20260603140000_escala_100k.sql — RPCs p/ sync escalável + KPI tempo médio (aditivo, idempotente).

-- (1) Filhas mudadas escopadas por empresa via JOIN no banco (substitui o array .in() em memória).
--     SECURITY DEFINER + revoke (mesmo padrão de sync_parent_in_empresa): só o service_role do
--     /api/sync/pull chama; nunca exposto a authenticated (que poderia passar qualquer empresa).
create or replace function public.sync_children_changed(
  p_table text, p_empresa uuid, p_cursor timestamptz, p_limit int
) returns setof jsonb language plpgsql stable security definer set search_path = public as $$
begin
  if p_table = 'pedido_pontos_retirada' then
    return query
      select to_jsonb(c) from public.pedido_pontos_retirada c
      join public.pedidos p on p.id = c.pedido_id
      where p.empresa_id = p_empresa and c.updated_at > p_cursor
      order by c.updated_at asc limit p_limit;
  elsif p_table = 'pedido_itens' then
    return query
      select to_jsonb(c) from public.pedido_itens c
      join public.pedido_pontos_retirada pr on pr.id = c.ponto_retirada_id
      join public.pedidos p on p.id = pr.pedido_id
      where p.empresa_id = p_empresa and c.updated_at > p_cursor
      order by c.updated_at asc limit p_limit;
  elsif p_table = 'os_itens' then
    return query
      select to_jsonb(c) from public.os_itens c
      join public.ordens_servico o on o.id = c.os_id
      where o.empresa_id = p_empresa and c.updated_at > p_cursor
      order by c.updated_at asc limit p_limit;
  elsif p_table = 'os_servicos' then
    return query
      select to_jsonb(c) from public.os_servicos c
      join public.ordens_servico o on o.id = c.os_id
      where o.empresa_id = p_empresa and c.updated_at > p_cursor
      order by c.updated_at asc limit p_limit;
  else
    raise exception 'sync_children_changed: tabela nao suportada %', p_table;
  end if;
end $$;
revoke all on function public.sync_children_changed(text, uuid, timestamptz, int) from public, anon, authenticated;

-- (2) Subconjunto de p_ids que pertencem à empresa (checagem de pais em lote).
create or replace function public.sync_parents_in_empresa(
  p_table text, p_ids uuid[], p_empresa uuid
) returns setof uuid language plpgsql stable security definer set search_path = public as $$
begin
  if p_table = 'pedidos' then
    return query select id from public.pedidos where id = any(p_ids) and empresa_id = p_empresa;
  elsif p_table = 'ordens_servico' then
    return query select id from public.ordens_servico where id = any(p_ids) and empresa_id = p_empresa;
  elsif p_table = 'pedido_pontos_retirada' then
    return query
      select pp.id from public.pedido_pontos_retirada pp
      join public.pedidos p on p.id = pp.pedido_id
      where pp.id = any(p_ids) and p.empresa_id = p_empresa;
  else
    return; -- nenhum
  end if;
end $$;
revoke all on function public.sync_parents_in_empresa(text, uuid[], uuid) from public, anon, authenticated;

-- (3) KPI tempo médio "pendente→finalizado" (horas), agregado no banco. INVOKER + grant (RLS escopa empresa).
create or replace function public.admin_tempo_medio_horas()
returns numeric language sql stable security invoker as $$
  select avg(extract(epoch from (ev.created_at - p.created_at)) / 3600.0)::numeric
  from public.pedido_eventos ev
  join public.pedidos p on p.id = ev.pedido_id
  where ev.tipo = 'status_change' and (ev.payload->>'to') = 'finalizado' and p.deleted_at is null;
$$;
grant execute on function public.admin_tempo_medio_horas() to authenticated;
```

- [ ] **Step 2: Validar sintaxe**

Run: `node -e "const s=require('fs').readFileSync('supabase/migrations/20260603140000_escala_100k.sql','utf8'); ['sync_children_changed','sync_parents_in_empresa','admin_tempo_medio_horas'].forEach(f=>{if(!s.includes('function public.'+f))throw new Error('faltou '+f)}); if((s.match(/security definer/gi)||[]).length!==2) throw new Error('esperado 2 DEFINER'); console.log('OK migration 100k')"`
Expected: `OK migration 100k`

- [ ] **Step 3: Tipos em `lib/types/database.ts`** (adicionar no bloco `Functions: {`, junto dos outros)

```ts
      sync_children_changed: {
        Args: { p_table: string; p_empresa: string; p_cursor: string; p_limit: number }
        Returns: Json[]
      }
      sync_parents_in_empresa: {
        Args: { p_table: string; p_ids: string[]; p_empresa: string }
        Returns: string[]
      }
      admin_tempo_medio_horas: { Args: never; Returns: number }
```

- [ ] **Step 4: Verify + commit**

Run: `npm run typecheck` (exit 0).
```bash
git add supabase/migrations/20260603140000_escala_100k.sql lib/types/database.ts
git commit -m "feat(escala-100k): RPCs de escopo do sync (children/parents) + KPI tempo medio"
```

---

## Task 2: `supabase-db.ts` — selectChanges via RPC + métodos em lote

**Files:**
- Modify: `lib/sync/supabase-db.ts`

- [ ] **Step 1: Trocar o escopo das filhas no `selectChanges` pela RPC**

LER o arquivo. No `selectChanges`, substituir TODO o bloco de ramos de filhas (os `else if (table === 'pedido_pontos_retirada')` / `'pedido_itens'` / `'os_itens'||'os_servicos'` que usam `pedidoIdsDaEmpresa`/`pontoIdsDaEmpresa`/`osIdsDaEmpresa` + `.in()`) por uma chamada à RPC quando a tabela NÃO tem empresa_id direto:
```ts
    async selectChanges(table, empresaId, cursor, limit) {
      if (!hasDirectEmpresaId(table)) {
        const { data, error } = await supabase.rpc('sync_children_changed', {
          p_table: table, p_empresa: empresaId, p_cursor: cursor, p_limit: limit,
        });
        if (error) throw error;
        return (data ?? []) as Row[];
      }
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const { data, error } = await (supabase as any)
        .from(table).select('*')
        .gt('updated_at', cursor)
        .eq(scopeColumn(table), empresaId)
        .order('updated_at', { ascending: true })
        .limit(limit);
      if (error) throw error;
      return (data ?? []) as Row[];
    },
```
Remover as funções `pedidoIdsDaEmpresa`, `pontoIdsDaEmpresa`, `osIdsDaEmpresa` (não usadas mais).

- [ ] **Step 2: Adicionar `findCanonicalMany` + `parentsInEmpresa` ao objeto retornado por `makeSupabaseSyncDb`**

Inserir (junto aos outros métodos):
```ts
    async parentsInEmpresa(parentTable, parentIds, empresaId) {
      const set = new Set<string>();
      const ids = [...new Set(parentIds.map((x) => x as string).filter(Boolean))];
      if (ids.length === 0) return set;
      const { data, error } = await supabase.rpc('sync_parents_in_empresa', {
        p_table: parentTable, p_ids: ids, p_empresa: empresaId,
      });
      if (error) throw error;
      for (const id of (data ?? []) as string[]) set.add(String(id));
      return set;
    },

    async findCanonicalMany(table, empresaId, pks) {
      const map = new Map<string, Row>();
      const ids = [...new Set(pks.map((x) => x as string).filter(Boolean))];
      if (ids.length === 0) return map;
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      let q = (supabase as any).from(table.name).select('*').in(table.pk, ids);
      if (hasDirectEmpresaId(table.name)) q = q.eq(scopeColumn(table.name), empresaId);
      const { data, error } = await q;
      if (error) throw error;
      let rows = (data ?? []) as Row[];
      if (!hasDirectEmpresaId(table.name) && table.parent) {
        const parentIds = rows.map((r) => r[table.parent!.fk]);
        const valid = await this.parentsInEmpresa(table.parent.table, parentIds, empresaId);
        rows = rows.filter((r) => valid.has(String(r[table.parent!.fk])));
      }
      for (const r of rows) map.set(String(r[table.pk]), r);
      return map;
    },
```

- [ ] **Step 3: Verify + commit**

Run: `npm run typecheck` (exit 0 — note: os tipos de `SyncDb` mudam na Task 3; se reclamar de método faltando na interface, ok, a Task 3 adiciona à interface. Ordem: faça Task 3 junto OU adicione a assinatura na interface primeiro. Recomendado: aplicar Task 3 Step 1 (interface) ANTES do typecheck aqui).
```bash
git add lib/sync/supabase-db.ts
git commit -m "perf(sync): escopo de filhas via RPC + findCanonicalMany/parentsInEmpresa (sem array em memoria)"
```

---

## Task 3: `engine.ts` — SyncDb +2 métodos, runPush em lote (TDD)

**Files:**
- Modify: `lib/sync/engine.ts`
- Test: `lib/sync/__tests__/engine.test.ts`, `lib/sync/__tests__/scenarios.test.ts`

- [ ] **Step 1: Adicionar os 2 métodos à interface `SyncDb`** (em `lib/sync/engine.ts`, dentro de `export type SyncDb = {`)

```ts
  /** Canônicas por PK (em lote), escopadas à empresa. Mesmo critério do findCanonical. */
  findCanonicalMany(table: SyncTable, empresaId: string, pks: unknown[]): Promise<Map<string, Row>>;
  /** Subconjunto de parentIds que pertencem à empresa (checagem de pais em lote). */
  parentsInEmpresa(parentTable: string, parentIds: unknown[], empresaId: string): Promise<Set<string>>;
```

- [ ] **Step 2: Atualizar o fake DB de `engine.test.ts`** (dentro de `makeDb`, no objeto `db`)

```ts
    async findCanonicalMany(table: SyncTable, empresaId, pks) {
      const map = new Map<string, Row>();
      for (const pk of pks) {
        const found = await this.findCanonical(table, empresaId, pk);
        if (found) map.set(String(pk), found);
      }
      return map;
    },
    async parentsInEmpresa(parentTable, parentIds, empresaId) {
      const set = new Set<string>();
      for (const id of parentIds) {
        if (parentEmpresa[parentTable]?.[String(id)] === empresaId) set.add(String(id));
      }
      return set;
    },
```

- [ ] **Step 3: Atualizar o fake DB de `scenarios.test.ts`** (mesmo objeto/2 métodos — LER o arquivo; o fake tem `parentEmpresa` análogo. Se a forma diferir, espelhe usando os `findCanonical`/`parentBelongsToEmpresa` já existentes do próprio fake):

```ts
    async findCanonicalMany(table: SyncTable, empresaId, pks) {
      const map = new Map<string, Row>();
      for (const pk of pks) {
        const f = await this.findCanonical(table, empresaId, pk);
        if (f) map.set(String(pk), f);
      }
      return map;
    },
    async parentsInEmpresa(parentTable, parentIds, empresaId) {
      const set = new Set<string>();
      for (const id of parentIds) {
        if (await this.parentBelongsToEmpresa(parentTable, id, empresaId)) set.add(String(id));
      }
      return set;
    },
```

- [ ] **Step 4: Escrever o teste de equivalência (TDD — falha antes do refactor do runPush)** em `engine.test.ts`

```ts
it('runPush em lote produz o MESMO resultado que o per-row (mesmas linhas gravadas)', async () => {
  const empresa = 'E1';
  const seed = {
    pedidos: [
      { id: 'p1', empresa_id: empresa, updated_at: '2026-01-01T00:00:00Z', field_updated_at: {} },
    ],
  };
  const incoming = {
    pedidos: [
      { id: 'p1', empresa_id: empresa, cliente_nome: 'Novo', updated_at: '2026-02-01T00:00:00Z',
        field_updated_at: { cliente_nome: '2026-02-01T00:00:00Z' } },
      { id: 'p2', empresa_id: empresa, cliente_nome: 'Outro', updated_at: '2026-02-01T00:00:00Z',
        field_updated_at: { cliente_nome: '2026-02-01T00:00:00Z' } },
    ],
  };
  const db = makeDb(seed);
  const r = await runPush(db, empresa, incoming);
  // p1 mergeado (cliente_nome novo vence pelo field_updated_at) e p2 inserido
  expect(r.applied.pedidos.map((x) => x.id).sort()).toEqual(['p1', 'p2']);
  expect(r.applied.pedidos.find((x) => x.id === 'p1')!.cliente_nome).toBe('Novo');
});
```
> NOTA: ajuste o shape de `PushResult` ao real — confira como `runPush` devolve as linhas aplicadas
> (campo `applied`/`result`) lendo `lib/sync/engine.ts:100-120`; use o nome correto no `expect`.

- [ ] **Step 5: Rodar — deve PASSAR já (o runPush atual é per-row e correto); este teste é a âncora de equivalência**

Run: `npx vitest run lib/sync/__tests__/engine.test.ts`
Expected: PASS (incluindo o novo). Se o shape do `PushResult` estiver errado no teste, corrija o teste até passar com o runPush ATUAL — ele é a referência.

- [ ] **Step 6: Refatorar `runPush` pra pré-buscar em lote** (em `lib/sync/engine.ts`, dentro do `for (const [name, rows] of ...)`, ANTES do `for (const raw of rows)`)

Inserir a pré-busca e trocar as chamadas per-row por leitura dos Maps/Sets:
```ts
      const t = SYNC_TABLES.find((x) => x.name === name)!;
      const result: Row[] = [];

      // Pré-busca em lote (leitura) — sem alterar a semântica de merge abaixo.
      const pks = rows.map((r) => r[t.pk]).filter((x) => x != null);
      const canonMap = await db.findCanonicalMany(t, empresaId, pks);
      const validParents = t.parent
        ? await db.parentsInEmpresa(t.parent.table, rows.map((r) => r[t.parent!.fk]), empresaId)
        : new Set<string>();
```
E DENTRO do `for (const raw of rows)`:
- Trocar `const ok = await db.parentBelongsToEmpresa(t.parent.table, parentId, empresaId);` por
  `const ok = validParents.has(String(parentId));`
- Trocar `const canonica = pkVal != null ? await db.findCanonical(t, empresaId, pkVal) : null;` por
  `const canonica = pkVal != null ? (canonMap.get(String(pkVal)) ?? null) : null;`
- Deixar `findCanonicalGlobal` e `upsertRaw` como estão (per-row).

- [ ] **Step 7: Rodar TODA a suíte de sync (a rede de segurança da semântica de merge)**

Run: `npx vitest run lib/sync/__tests__/`
Expected: `merge.test.ts`, `scenarios.test.ts`, `engine.test.ts` TODOS verdes (incluindo o de equivalência). Se algum quebrar, a pré-busca alterou comportamento → reverter o Step 6 e investigar.

- [ ] **Step 8: typecheck + commit**

Run: `npm run typecheck` (exit 0).
```bash
git add lib/sync/engine.ts lib/sync/__tests__/engine.test.ts lib/sync/__tests__/scenarios.test.ts
git commit -m "perf(sync): runPush pre-busca canonicas/pais em lote (merge intacto, suite verde)"
```

---

## Task 4: Export CSV em streaming

**Files:**
- Modify: `app/(app)/historico/export/route.ts`

- [ ] **Step 1: LER o arquivo inteiro** (precisa preservar os mesmos headers/colunas/formatação do CSV — só muda COMO os dados são lidos/escritos).

- [ ] **Step 2: Exigir filtro de data quando status não-terminal + paginar em streaming**

Substituir a parte que faz `.limit(50_000)` + `await query` + monta string única por: (a) validação — se `status` ∈ {todos, rascunho, pendente, em_separacao, parcialmente_entregue} e faltar `from`/`to`, retornar 400 `{ error: 'Informe um período (from/to) para exportar este status' }`; (b) um `ReadableStream` que escreve o header e depois lê em páginas de 1000 via `.range(offset, offset+999)` até vir menos que 1000, escrevendo cada linha. A função que monta UMA linha de CSV (escape de aspas/; etc.) deve ser extraída pra ser reusada por linha. Resposta:
```ts
  return new Response(stream, {
    headers: {
      'content-type': 'text/csv; charset=utf-8',
      'content-disposition': `attachment; filename="pedidos-${status}.csv"`,
    },
  });
```
> Mantém RLS (usa o client de sessão via `createClient`), os mesmos campos do select (sem o `.limit(50000)`), e a mesma ordem `created_at desc`. O encoder: `new TextEncoder()`; escreva o header primeiro, depois o loop de páginas.

- [ ] **Step 3: Verify + commit**

Run: `npm run typecheck` (exit 0). Run: `npm run lint -- "app/(app)/historico/export/route.ts"` (sem novo erro).
```bash
git add "app/(app)/historico/export/route.ts"
git commit -m "perf(export): CSV em streaming paginado + exige periodo p/ status nao-terminal"
```

---

## Task 5: KPI tempo médio via RPC (admin)

**Files:**
- Modify: `app/(app)/admin/page.tsx`

- [ ] **Step 1: Substituir o cálculo de `tempoMedioHoras`**

LER o arquivo. Remover o bloco que busca `pedido_eventos` (`.eq('tipo','status_change').limit(5000)`), filtra `payload.to==='finalizado'`, faz a 2ª query `.in('id', pedidoIds)` e calcula a média em JS. Trocar por:
```ts
  const { data: tempoMedioHoras } = await supabase.rpc('admin_tempo_medio_horas');
```
> `tempoMedioHoras` agora é `number | null` (a RPC retorna `numeric`). Ajustar o uso no JSX (onde formata as horas) pra `tempoMedioHoras ?? null`. Remover a query de `pedido_eventos` do `Promise.all` (e o destructuring `{ data: eventosFinalizado }`), já que não é mais usada.

- [ ] **Step 2: Verify + commit**

Run: `npm run typecheck` (exit 0). Run: `grep -n "limit(5000)" "app/(app)/admin/page.tsx"` → vazio.
```bash
git add "app/(app)/admin/page.tsx"
git commit -m "perf(kpi): tempo medio pendente->finalizado agregado no banco (RPC)"
```

---

## Task 6: Gates finais + verificação adversarial

- [ ] **Step 1: Suíte + typecheck completos**

Run: `npm run test` (0 failed). Run: `npm run typecheck` (exit 0).

- [ ] **Step 2: Verificação adversarial (ultracode)** — dispatch de revisores independentes checando:
  (a) `sync_children_changed` retorna EXATAMENTE as mesmas linhas que o antigo `.in(idsDaEmpresa)` (mesma
  ordem por updated_at, mesmo cursor, mesmas filhas escopadas); (b) o `runPush` em lote preserva a semântica
  de merge linha-a-linha (canônica certa por PK; parent check idêntico; INSERT vs UPDATE idêntico); (c) as
  RPCs DEFINER estão revogadas de `authenticated` (sem vazamento cross-empresa).

- [ ] **Step 3: SQL pra nuvem** — gerar o bloco do `20260603140000_escala_100k.sql` pro usuário colar no Supabase.

---

## Self-Review (autor)
- **Cobertura do spec:** #1 sync memória (T1 RPC + T2 wiring) ✓; #2 push lote (T3, TDD) ✓; #3 export streaming (T4) ✓; #4 KPI tempo médio (T1 RPC + T5 wiring) ✓; retenção/trgm/cold-start fora ✓.
- **Placeholders:** SQL e código completos. Os 2 pontos com "LER o arquivo" (export route, admin) trazem
  instrução exata + o trecho a trocar; o shape do `PushResult` no teste tem nota de conferência (não é placeholder — é precaução de exatidão).
- **Consistência:** `sync_children_changed`/`sync_parents_in_empresa`/`admin_tempo_medio_horas` definidas em T1,
  tipadas em T1, usadas em T2/T3/T5 com os mesmos nomes/args; `findCanonicalMany`/`parentsInEmpresa` na interface (T3 Step1) batem com a impl (T2 Step2) e o fake (T3 Step2/3); padrão de segurança DEFINER+revoke igual ao `sync_parent_in_empresa`.
- **Risco:** T3 é o único de semântica — protegido por TDD (teste de equivalência + suíte merge/scenarios) e verificação adversarial (T6).
