{
  "_comment": "Exemplo do config.json do hub Exped (Windows). O instalador (Inno Setup) copia isto para C:\\Exped\\config.json e SUBSTITUI o jwtSecret por um valor aleatorio real (gerado no install). O install-service.ps1 le este arquivo e injeta cada chave como env var EXPED_* do servico ExpedHub (ver hub/config.mjs e hub/maestro.mjs). Paths usam barra dupla por ser JSON.",

  "ports": {
    "pg": 54329,
    "postgrest": 54331,
    "gotrue": 9999,
    "gateway": 54320,
    "storage": 5402,
    "app": 3000
  },

  "paths": {
    "_comment_pgData": "DIRETORIO DE DADOS do cluster Postgres. O maestro faz pg_ctl -D <pgData> start. No Windows nao ha socket Unix, entao data dir e host de conexao sao SEPARADOS.",
    "pgData": "C:\\Exped\\data\\pg",
    "_comment_pgHost": "HOST DE CONEXAO TCP usado por psql/PostgREST/GoTrue/app. No Windows e sempre 127.0.0.1 (sem socket Unix).",
    "pgHost": "127.0.0.1",
    "db": "exped",
    "user": "postgres"
  },

  "_comment_jwtSecret": "PLACEHOLDER. O instalador gera um segredo aleatorio (>=32 chars) e grava o config.json real com ele. As chaves anon/service_role do app derivam deste segredo (HS256) em runtime.",
  "jwtSecret": "TROCAR-no-install-por-segredo-aleatorio-min-32-chars",

  "_comment_manifestUrl": "URL do manifesto de auto-update { versao, url, sha256 } no bucket publico hub-releases. null desliga. O hub baixa, valida sha256, aplica migrations aditivas e reinicia o app (com rollback).",
  "manifestUrl": "https://louaguxcohfeicxxqggw.supabase.co/storage/v1/object/public/hub-releases/manifest.json"
}
